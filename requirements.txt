# Core
python-dotenv>=1.0.0

# Groq API (D-016) — generator and judge LLM
groq>=0.9.0
langchain-groq>=0.1.0
langchain>=0.1.0
langchain-community>=0.0.10

# Local embeddings — BAAI/bge-large-en-v1.5 (D-003)
sentence-transformers>=2.2.2
transformers>=4.35.0
huggingface-hub>=0.19.0
langchain-huggingface>=0.0.6    # LangChain wrapper used by RAGAS embeddings

# Vector store
faiss-cpu>=1.7.4                # swap for faiss-gpu on CUDA machines

# BM25 for hybrid RAG
rank-bm25>=0.2.2

# Graph RAG
spacy>=3.7.0
networkx>=3.1

# Evaluation
ragas>=0.1.0
datasets>=2.14.0

# Data fetching
wikipedia-api>=0.6.0
arxiv>=2.0.0
requests>=2.31.0

# System metrics
psutil>=5.9.0

# Data processing
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.65.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.15.0

# Token counting (improves chunk sizing accuracy)
tiktoken>=0.5.0

# PyTorch — pre-installed on Colab; install manually on local machines
# See https://pytorch.org/get-started/locally/ to pick the right CUDA version
torch>=2.0.0

# ── Optional: OpenAI (D-013 embedding comparison only) ───────────────────────
# Only needed if you want to benchmark OpenAI embedding models alongside bge-*.
# The core benchmark runs entirely on Groq + local HuggingFace embeddings.
#
# openai>=1.0.0
# langchain-openai>=0.0.5
